import AddContactForm from "../components/Forms/AddContactForm";

export default function AddContactIndex() {
  return (
    <section className="add-contact">
      <h1>Add Contact</h1>

      <AddContactForm />
    </section>
  );
}
